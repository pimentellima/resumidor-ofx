export type Statement = {
    amount: number
    description: string
    date: string
    bank_id: string
    balance_amount: number
    fit_id: string
    transaction_id?: string
    category: string
}
